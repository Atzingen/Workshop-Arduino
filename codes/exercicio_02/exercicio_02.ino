/*
    Workshop de Arduino

    Semana da computação 2017
    IFSP Campus Piracicaba

    -----------------------------
    Exercicio 02

    Faça um programa que ligue o rele quando o som ambiente ficar muito alto.
    Na primeira vez, ligue o rele por 2 segundos, na segunda vez por 4 segundos
    e depois disso, ligue por 8 segundos cada vez que o som ambiente ficar
    muito alto.
*/

// Defina a variavel para o pino do rele
// Defina a variavel para o pino do sensor de som
// Defina uma variavel para contar o numero de vezes que o
// som ficou alto

void setup()
{
    // defina o pino do rele como saida
}

void loop()
{
    // Leita a altura do som ambiente
    // Se for alta de mais, incremente a variavel contador do n de vezes
    // Atue de acordo caso o som seja alto. (2 , 4 ou 8 segundos)
}
